# Rules from .gitignore apply
#!include:.gitignore

# ...but the per-deploy config overlay (gitignored) MUST reach the build image.
!config/local-production.yaml

# Not needed to build the image
test/
*.md
.nvmrc
.bowerrc
.agignore
Vagrantfile
deploy-cloudrun.sh
cleanup-cloudrun.sh
scripts/
SETUP.md
docker-compose.yml
