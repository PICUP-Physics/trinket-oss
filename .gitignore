*.DS_Store
.env
config/firebase-service-account.json

node_modules
public/components
public/vendor
public/404.html
public/50x.html
public/maintenance.html

*.sublime-project
*.sublime-workspace

# Local environment secrets (Garage/S3 credentials, etc.)
.env

Vagrantfile
.vagrant

config/runtime.json
config/runtime.json.tmp-*
config/firebase-service-account.json
config/development.yaml
config/development.yml
config/development.json
config/production.yaml
config/production.yml
config/production.json
config/local.yaml
config/local.yml
config/local.json
config/migration.json
config/migrationDb.json

npm-debug.log
build.log
test-*.js

.sass-cache

public/css/base.css
public/css/base.css.map
public/css/embed.css
public/css/embed.css.map
public/css/publish
public/js/dist
lib/views/courses/download/course.css
lib/views/courses/download/course.css.map

lib/migrations/backups

public/_published

*.pyc

*.swp

.agignore

.claude
*~

*.log

lti-private-key.pem
firestore-debug.log

# local, untracked runbooks/notes
*.local.md

# never commit env backups (contain secrets)
.env.bak
*.env.bak

# per-deploy config overlay — lives in each deploy worktree, never committed.
# (Un-ignored in .gcloudignore so it still reaches the Cloud Build image.)
config/local-production.yaml
config/local-production-cloudrun.yaml

# Per-deploy brand assets (logos) — each worktree carries its own; never shared
public/img/brand/
